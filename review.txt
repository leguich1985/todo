*1. Вынести моки из контейнера
*2. Добавить файл конфига чтобы делать импорт от корня, то есть не '../../components/', а 'components/'
*3. Функции добавления задачи, удаления задачи и прочие сделать чистыми. То есть, функции должны делать return, а не обновлять стейт внутри себя.
*4. Не нужно называть функции с on (onDelete...)
*5. Используй деструктуризацию, например, на event => { target: { name, value } }
*6. В контейнере из useEffect фильтрацию вынести в отдельную функцию 
*7. Не нужно СРАЗУ генерировать инпут для редактирования, подставляй инпут тому элементу, который в данный момент редактируется 
*8. Компонент Task, вынести логику из компонента в контейнер, наверх прокидывать только параметры для данных функций.
*9. Компонент TaskList прокидывать пропсы спредом перед передачей остальных пропсов, иначе ты можешь их затереть спредом
*10. Компонент TasksFilter вынести логику из компонента

//------------------------------------------------------------------

*1. TasksFilter убрать return
*2. В src создать папку constants и хранить в ней константы
*3. Имена констант в папке constants называть по типу UPPER_CASE
*4. Стили хранить рядом с их компонентами/ui (пример, Button)
*5. itemsLeft - дать более осмысленное имя переменной, фильтр импортировать как отдельную функцию, саму по себе функцию вынести в отдельный хелпер и написать на нее тест
*6. onDeleteTodo - вынести в отдельную функцию фильтрацию и саму функцию, оптимизировать функцию через useCallback, написать тесты
*7. filteredTodos - вынести в отдельную функцию, написать на нее тесты
(пока что не надо)8. Добавить лоадер на добавление, удаление, изменение и фильтрацию задач (лоадер перекрывает весь список в таком случае)
*9. Добавить предохранитель (вместо списка todo будет показывать сообщение: что-то пошло не так. И кнопку с текстом Обновить страницу) - пока отмена
10. Экшены фильтров вынести из объекта и протестировать
*11. При заходе на страницу, если нет задачи в статусе на редактирование, устанавливать фокус на инпут добавления

//------------------------------------------------------------------

1. Удалить установку фокуса через js
2. У контейнеров нет стилей
3. Вынести получение полей туду в отдельную функцию, часть полей вынести в константу, написать тесты на новую функцию
4. Дать более осмысленные имена todos, activetodos и остальным